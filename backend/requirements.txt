# backend/requirements.txt
fastapi>=0.90.0
uvicorn[standard]>=0.20.0
# Add the async driver
asyncpg>=0.28.0
# OR, if you prefer synchronous:
# psycopg2-binary>=2.9.0

# Recommended for loading .env files during local development
python-dotenv>=1.0.0